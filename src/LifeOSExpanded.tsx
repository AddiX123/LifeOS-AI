import {useEffect,useState} from 'react';
import {api,auth} from '@appdeploy/client';
import {Home,CalendarDays,MessageCircle,Target,HeartPulse,Search,Library,ShieldCheck,Settings,Menu,Sun,Moon,Sparkles,ChevronRight,CircleUserRound,Plus,X} from 'lucide-react';
import AskLifeOSV2 from './AskLifeOSV2';import DayMastery from './DayMastery';import ResearchV23 from './ResearchV23';import LibraryV23 from './LibraryV23';import PrivacyV23 from './PrivacyV23';import SettingsV23 from './SettingsV23';import './aurora.css';import './v23.css';
type Section='today'|'day'|'ask'|'goals'|'health'|'research'|'library'|'privacy'|'settings';
const nav=[['today','Today',Home],['day','Day Plan',CalendarDays],['ask','Ask LifeOS',MessageCircle],['goals','Life Goals',Target],['health','Health & Care',HeartPulse],['research','Research',Search],['library','Library',Library],['privacy','Privacy',ShieldCheck],['settings','Settings',Settings]] as const;
export default function LifeOSExpanded(){const[section,setSection]=useState<Section>('today'),[dark,setDark]=useState(true),[mobile,setMobile]=useState(false),[user,setUser]=useState<any>(null),[messages,setMessages]=useState<any[]>([]),[input,setInput]=useState(''),[files,setFiles]=useState<File[]>([]),[toast,setToast]=useState('');useEffect(()=>{void auth.getUser().then(setUser)},[]);const go=(s:Section)=>{setSection(s);setMobile(false)};const ask=async(q:string,attachments=files,mode='auto')=>{if(!q.trim())return;const next=[...messages,{role:'user',content:q,files:attachments.map(f=>f.name)}];setMessages(next);setInput('');try{const media:any[]=[];for(const f of attachments){if(f.type.startsWith('image/')||f.type.startsWith('audio/')){const data=await new Promise<string>((res,rej)=>{const r=new FileReader();r.onload=()=>res(String(r.result).split(',')[1]||'');r.onerror=rej;r.readAsDataURL(f)});media.push({data,mimeType:f.type,name:f.name,kind:f.type.startsWith('image/')?'image':'audio'})}}const r=await api.post('/api/ai/chat',{message:q,history:next.slice(-12),media,memoryOn:true,mode});setMessages([...next,{role:'assistant',content:r.data?.text||'No answer returned.'}]);return r.data?.text||''}catch(e){const msg=e instanceof Error?e.message:'Request failed.';setMessages([...next,{role:'assistant',content:`I couldn't process that request. ${msg}`}]);return ''}};
return <div className={dark?'app dark':'app'}><aside className={mobile?'sidebar open':'sidebar'}><div className="brand"><div className="brand-mark"><Sparkles size={18}/></div><div><strong>LifeOS</strong><span>HUMAN INTELLIGENCE & CARE OS</span></div></div><button className="close-mobile" onClick={()=>setMobile(false)}><X/></button><div className="workspace-label">YOUR LIFE</div><nav>{nav.map(([id,label,Icon])=><button key={id} className={section===id?'nav-item active':'nav-item'} onClick={()=>go(id)}><Icon size={17}/><span>{label}</span></button>)}</nav><div className="sidebar-bottom"><button className="new-chat-side" onClick={()=>go('ask')}><Plus size={15}/> New Chat</button></div></aside><main className="main"><header className="topbar"><button className="mobile-menu" onClick={()=>setMobile(true)}><Menu/></button><div className="breadcrumb"><span>LifeOS</span><ChevronRight size={14}/><b>{nav.find(x=>x[0]===section)?.[1]}</b></div><div className="top-actions"><button className="icon-button" onClick={()=>setDark(x=>!x)}>{dark?<Sun size={17}/>:<Moon size={17}/>}</button>{user?<div className="profile"><div className="avatar">{user.picture?<img src={user.picture} alt=""/>:<CircleUserRound size={19}/>}</div><span>{user.name?.split(' ')[0]||'You'}</span></div>:<span className="profile">Guest</span>}</div></header><div className="content">{section==='today'&&<div className="page"><div className="hero-row"><div><div className="eyebrow"><Sparkles size={13}/> NEXT BEST ACTION</div><h1>Protect your focus before noon<span className="gradient-dot">.</span></h1><p className="lede">Anchor the day around your highest-value goal, energy and constraints.</p></div></div></div>}{section==='day'&&<DayMastery tracker={[]} setTracker={()=>{}} metrics={[]} setMetrics={()=>{}} toast={setToast}/>} {section==='ask'&&<AskLifeOSV2 messages={messages} input={input} setInput={setInput} files={files} setFiles={setFiles} ask={ask} newChat={()=>{setMessages([]);setInput('')}}/>}{section==='goals'&&<div className="page narrow"><h1>Life Goals</h1><p className="lede">Turn important goals into practical plans with Ask LifeOS.</p></div>}{section==='health'&&<div className="page narrow"><div className="page-head"><div className="eyebrow"><HeartPulse size={13}/> HEALTH & CARE</div><h1>Your health, organized.</h1></div></div>}{section==='research'&&<ResearchV23 onAsk={q=>{go('ask');setInput(q)}}/>}{section==='library'&&<LibraryV23 onOpenChat={()=>go('ask')}/>} {section==='privacy'&&<PrivacyV23/>}{section==='settings'&&<SettingsV23 user={user} signIn={()=>auth.signIn({scope:'openid email profile offline_access'}).then(r=>setUser(r.user))} signOut={()=>auth.signOut().then(()=>setUser(null))} onToast={setToast}/>}</div>{toast&&<button className="toast" onClick={()=>setToast('')}>{toast}</button>}</main></div>}
