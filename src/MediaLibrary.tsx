import {useEffect,useState} from 'react';import {Image as ImageIcon,Trash2,Video} from 'lucide-react';import {api} from '@appdeploy/client';export default function MediaLibrary(){const[items,setItems]=useState<any[]>([]);useEffect(()=>{void api.get('/api/media').then(r=>setItems(r.data||[]))},[]);return <div className="media-library"><span className="eyebrow">CREATED WITH LIFEOS</span><h2>Images & videos</h2><div className="media-grid">{items.map(m=><article className="media-card" key={m.id}>{m.url&&m.status==='completed'?(m.type==='image'?<img src={m.url} alt={m.title}/>:<video src={m.url} controls playsInline/>):<div className="media-pending">{m.type==='video'?<Video/>:<ImageIcon/>}<b>{m.status}</b></div>}<div><b>{m.title}</b><button onClick={()=>api.delete(`/api/media/${m.id}`).then(()=>setItems(x=>x.filter(a=>a.id!==m.id)))}><Trash2 size={14}/></button></div></article>)}</div></div>}
